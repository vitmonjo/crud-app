import React from 'react';
import styles from '../styles/header.module.css'

const Header = () => {
  return (
    <header className={styles.header}>
      <h1>vitmonjo's - CRUD Application</h1>
    </header>
  );
};

export default Header;